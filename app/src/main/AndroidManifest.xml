<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad_cw">
    <!-- Enable Permissions In-App -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Splash / Welcome Screen -->
        <!-- Main Activity - Startup Activity -->
        <!-- Other Activities -->

        <!-- Base Activity -->
        <activity android:name=".BaseActivity" />

        <!-- User Activities -->
        <activity android:name=".ui.user.User_Auth_Activity" />
        <activity android:name=".ui.user.User_Profile_Activity" />

        <!-- Classified Ads Activities -->
        <activity android:name=".ui.user.User_Advert_Create_Edit_Activity" />
        <activity android:name=".ui.user.User_Adverts_Favourite_Activity" />
        <activity android:name=".ui.user.User_Adverts_Personal_Activity"/>
        <activity android:name=".ui.adverts.AdvertInfoActivity" />

    </application>

</manifest>